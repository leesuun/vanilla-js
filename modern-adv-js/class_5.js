/*
class Animal {
    constructor(name) {
        this._name = name;
    }
    bark() {
        return "짖다";
    }
}

class Dog extends Animal {
    constructor(name, age) {
        super(name);
        this._age = age;
    }
    bark() {
        return `${this._age}살짜리 ${this._name}가 짖다.`;
    }
}

const hari = new Dog("hari", 10);
console.log(hari.bark());
*/
